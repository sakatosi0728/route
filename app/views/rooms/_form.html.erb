<%= form_with(model: room, local: true) do |form| %>
  <%= form.text_field :image, placeholder: "イメージ画像" %>
  <%= form.text_area :text, placeholder: "説明文", rows: "10" %>
  <%= form.submit "作成する" %>
<% end %>